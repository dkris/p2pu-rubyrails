
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p136@p2pu-rails" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p136@p2pu-rails"
else
  rvm --create  "ruby-1.9.2-p136@p2pu-rails"
fi
